# Metropolis Liquidity Manager

Automated DLMM liquidity management for Metropolis on Sonic EVM Layer-1 Network, developed for cloud deployment.

## Setup

1. Clone this repository
2. Install dependencies: `pip install -r requirements.txt`
3. Configure environment variables
4. Run the app: `functions-framework-python --target=manage_liquidity`

## Cloud Configuration

